<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Users-Products</title>
    <link rel="stylesheet" href="/style.css">
    <link rel="stylesheet" href="/styles/products.css">
    <link rel="stylesheet" href="/styles/footer.css">
    <script src="https://cdn.jsdelivr.net/npm/axios/dist/axios.min.js"></script>
</head>

<body>
    {{>navbar isAdmin=isAdmin cartCnt=cartCnt}}
    <div class="container">
        <div class="productDetails">
            <div class="productItem">
                <div class="productImg">
                    <img src="{{product.imageUrl}}" alt="">
                </div>
                <div class="info">
                    <div class="productInfo">
                        <h2 class="name">
                            {{product.name}}
                        </h2>
                        <ul class="description">
                            <li>{{product.description}}</li>
                        </ul>
                    </div>
                    <div class="priceInfo">
                        <div class="price">
                            $ {{product.price}}
                        </div>
                        <div class="btnGrp">
                            <button class="btn">
                                <a href="/shop/buy-now?id={{product._id}}">Buy Now</a>
                            </button>
                            <button productId="{{product._id}}" class="btn addToCartBtn">
                                {{!-- <a href="#">Add To Cart</a> --}}
                                Add To Cart
                            </button>
                        </div>
                    </div>
                </div>
            </div>
        </div>
    </div>
    <button class="logoutBtn">
        <a href="/logout">Logout</a>
    </button>

    <div class="footerPartial">
        {{>footer}}
    </div>

    <script src="/js/addToCartjs.js"></script>
</body>

</html>